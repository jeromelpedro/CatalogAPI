apiVersion: v1
kind: ServiceAccount
metadata:
  name: catalog-api
  labels:
    app: catalog-api
    azure.workload.identity/use: "true"
  annotations:
    azure.workload.identity/client-id: "__AZURE_CLIENT_ID__"